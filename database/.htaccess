# Deny direct access to raw SQL schemas, dumps, and database artifacts
<FilesMatch "\.(sql|bak|dump|ini|json)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>
